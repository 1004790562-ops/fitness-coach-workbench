# 朝夕工作台 · 架构与续作指南（最终版 v8 · 2026-08-01）

> 本文件是「朝夕工作台」项目的**持久记忆（续作指南）**。当上下文将满、需要开新对话框继续完善时，
> **请在新对话框第一条消息里写：「继续完善朝夕工作台，先读 <本文件路径>」**，
> 我（WorkBuddy）读完后即可无缝接手，无需你重新介绍架构。
>
> 本文件已按 2026-08-01 收尾状态更新，覆盖 v4→v8 全部演进。**之前停留在 v4.3 的旧版指南已作废。**

---

## 0. 一句话定位

**朝夕工作台** = 一个单文件 HTML 网页（手机加到主屏幕即用），作为你的「手机端极速录入端」，
覆盖**健身训练 / 待办事项 / 日记（情绪·睡眠） / 护肤打卡**四大功能；数据沉淀进 Obsidian，统筹思考交给 WorkBuddy 大脑。

---

## 1. 三方架构（必须记住）

| 角色 | 工具 | 职责 | 数据落点 |
|---|---|---|---|
| **录入 / 执行** | 朝夕工作台（本网页） | 当下高频交互：日历总览、跟练打卡、写日记、记待做、护肤打卡、看仪表盘 | 浏览器 localStorage（key=`zhaoxi_workbench_v5`）+ GitHub Gist 云端兜底；源码在 CloudStudio |
| **沉淀 / 检索** | Obsidian 个人管理系统 | 持久沉淀：日记、八字项目、健身理论、双链、Dataview 聚合 | **本机权威 vault：`C:\Users\lenovo\ObsidianVault`**（2026-08-05 用户确认；含 `02-Daily · 每日记录/Journal日记/YYYY年/`、`06-Templates · 模板/日记模版.md`、`大脑每日同步指令.md`）。⚠️ iCloud 旧仓库（`C:\Users\lenovo\iCloudDrive\2、工作⛳️\个人管理系统` 及 `iCloud~md~obsidian\个人管理系统`）已废弃/删除，**不再是归档目标** |
| **思考 / 联动（唯一桥）** | WorkBuddy 大脑（桌面 agent） | 自然语言理解、汇总、改代码并部署、把工作台数据按归档指令写入 Obsidian | 临时读写，不留存 |

> ⚠️ 关键事实：网页**读不到**本机 Obsidian 文件，Obsidian 也读不到浏览器数据。两者**永不直接对话**，必须靠大脑做桥。

**联动口诀：工作台 = 录入执行，Obsidian = 沉淀检索，WorkBuddy = 思考联动。**

---

## 2. 文件位置（绝对路径）

- **工作台源码（唯一真源）**：`C:\Users\lenovo\WorkBuddy\2026-07-31-14-53-38\fitness-coach-workbench\index.html`
  - 同目录还有：
    - `朝夕工作台-架构与续作指南.md`（本文件）
    - `apple-touch-icon.png`（iOS 主屏幕大太阳图标，1024×1024，金橙）
    - `favicon.png`（浏览器标签小图标）
    - `manifest.json`（已不被 HTML 引用，保留无害）
    - `server.js`（Node 零依赖云同步后端，CloudStudio 纯静态托管跑不了，未运行）
- **Obsidian 联动约定**：`04-Areas · 领域/Body身体管理/健身/朝夕工作台-大脑联动约定.md`
- **Obsidian 日记体系**：`C:\Users\lenovo\ObsidianVault\02-Daily · 每日记录\Journal日记\YYYY年\2026-MM-DD.md`（字段对齐 `06-Templates · 模板\日记模版.md`）；登记簿 `Journal日记\日记.memory`
- **每日录入自动归档 Skill（WorkBuddy 侧）**：`obsidian-daily-archive` → `C:\Users\lenovo\.workbuddy\skills\obsidian-daily-archive\SKILL.md`。用户发 `# 2026` 开头的当日录入时，大脑加载它写入上述日记并在 `日记.memory` 登记。**这是「同步今日到 Obsidian」的固化流程。**
  - **Obsidian 备份副本**：`C:\Users\lenovo\ObsidianVault\02-Daily · 每日记录\Journal日记\日记skill\SKILL.md`（走 iCloud 同步，电脑丢失可恢复）。
  - **长期规矩（2026-08-05）**：任何项目用到的 skill，都存一份副本进 ObsidianVault，**按用途领域放进对应文件夹**（如日记类→`Journal日记/日记skill/`、八字类→玄学 Area、健身类→Body身体管理），不准放 vault 根目录乱放，防电脑丢失。
- **Obsidian 状态备份笔记**：`05-Resources · 资源库/Ag Study智能体学习/个人工作台/朝夕工作台.md`（本指南的 Obsidian 镜像，最终状态）
- **Obsidian 产品审计**：`05-Resources · 资源库/Ag Study智能体学习/个人工作台/产品审计报告.md`（v5→v6 修复记录，引用 `[[朝夕工作台]]`）

---

## 3. 部署网址（给用户的手机书签）

- **CloudStudio（主用，国内直连，网址稳定不变）**：
  `https://f58a0a16ac5443d8886cd0d07271acac.gz3.agentos-app.net`
  → 改完代码后重新部署 CloudStudio 会**复用同一沙箱**，网址不变，书签无需重加。
- ⚠️ **旧沙箱 `386cd0d07271acac.gz3.agentos-app.net` 已失效/为空，严禁再使用**（早期部署，造成过 Safari 有数据、主屏无数据的分裂现象）。用户记忆里若存了这个旧网址，一律替换为上面的新网址。
- **GitHub Pages（备用，从未成功推送，国内可能需翻墙）**：
  `https://ZhaoXi-Ops.github.io/fitness-coach-workbench/`（仓库 `ZhaoXi-Ops/fitness-coach-workbench`）。**实际未启用。**
- **GitHub Gist（仅存数据备份，不存代码）**：`zhaoxi-workbench.json`，仅 gist 权限 PAT（用户名 `ZhaoXi-Ops`）。明文 token 不在此记录，需用时向用户索取或重生成。

> 跨设备数据同步 = 两台设备都开同一 CloudStudio 网址 + 都启用同一 Gist 云端同步；或导出/导入备份 JSON。

---

## 4. 当前版本（v8）功能清单 —— 准确终态

- **底部 5 导航**：`日历` / `事项` / `健身` / `日记` / `AI机器人`
- **顶部右侧齿轮按钮** → `数据/设置` 页（不在底部导航里，从顶栏进）
- **日历页**：月历网格 + 打点（`日/周/月/练/护` 五类，**无季/年**）+ 选中日详情三段（事项｜训练｜护肤）+ 切换月份默认选中（上一月→该月最后一天；下一月→1 号）+ 「今天」按钮回到当天
- **事项页**：日 / 周 / 月 / 季 / 年 五级任务（周任务有「➕ 加入今日」按钮）；日历打点只展示日/周/月三层，季/年仅在此列表内管理
- **健身页**：训练选择 + 打卡 + 取消打卡（取消有二次确认）
- **日记页**：心情 emoji / 睡眠（格式 `🛏️ —:— → ☀️ —:— · 🔋 —/10`）/ 自由日记 / 感恩三件事 / 输入·收获 / 明日计划；输入即存
- **AI机器人页**：①「📋 复制今日内容（发给大脑归档）」按 Obsidian 日记格式汇编当日 → 复制 → 用户发给大脑说「同步今日到 Obsidian」→ 大脑写入对应每日日记；②「📥 接收大脑回传」粘贴 WorkBuddy 回传文本（按 `[待做]/[感恩]/[睡眠]/[心情]/[护肤]` 标签）→ 自动归类写入对应字段
- **数据/设置页**：本期完成率、连续打卡、本周打卡、近 7 天打卡/心情/睡眠趋势图、🧴 护肤统计（本周护肤天/连续/基础流程完成率 + 条形图）；底部「云同步」含：启用/断开、复制令牌、下载备份 JSON、从备份恢复

---

## 5. 数据模型

```
LS_KEY = 'zhaoxi_workbench_v5'   // v4 数据首次加载会自动迁移补字段
{
  goal:{name,period,logic},
  planStart, plan:{name,totalDays,freq,phases:[...]},
  videos:[{id,platform,title,url,improve,goal,caution,duration}],
  checkins:[{date,videoId,planDay,feel,note}],
  diary:{ 'YYYY-MM-DD':{moodVal,moodEmoji,sleep,content,gratitude,input,tomorrow} },
  todos:{ 'YYYY-MM-DD':[{id,text,done,source}] },
  tasks:{ daily:[...], weekly:[...], quarter:[...], yearly:[...] },   // 任务对象自带 date 字段 + parentId
  skincareItems:[{id,name}],          // 固定 3 项：水 / 精华 / 其他
  skincareCheckins:[{date,itemId,feel,note}]
}
```
- 改数据结构时**保持向后兼容**（`normData()` 补缺省值，v4→v5 自动迁移）。

---

## 6. 视觉风格（用户明确偏好，不要回退）

- **B 活力双色粗线风**：主色 `--brand:#4c8df7`（鲜活蓝，非灰蓝）；背景微蓝 `--bg:#edf1f8`；正文/次要文字/描边加深（`--ink:#2B2D42`）；`--ok/--warn/--mood` 提饱和度。
- 导航图标深色粗描边（`stroke-width:2.6`）+ 一个品牌蓝点缀元素；选中态 = 浅蓝胶囊底 + 蓝字 + 加粗。
- 日历格放大（42px）、圆点放大（4.5px）、各标题字号加粗。
- 用户**明确不喜欢**「寡淡 / 线太细 / 字太小 / 灰蒙蒙」的风格，后续改动别回到那种。

---

## 7. iOS / PWA 关键事实（血泪踩坑，必读）

- 独立 App 模式**已启用**（`apple-mobile-web-app-capable` + `theme-color:#4c8df7`）。
- **防缓存双保险**：
  1. `<meta http-equiv="Cache-Control" content="no-cache,no-store,must-revalidate">` + Pragma + Expires
  2. JS 顶部 `const APP_VERSION='2026-08-01-v8'`——**每次部署改版本号**，旧 PWA 打开时检测到版本不匹配 → 自动 `location.reload(true)` 强制刷新服务器最新版
- **数据同步靠 GitHub Gist 云端**：Safari 与主屏图标读的是同一份云数据，避免 iOS 给独立 App 分配隔离 localStorage 仓库导致「主屏无数据」。
- 设置页「**复制令牌**」按钮：从有数据的窗口一键复制 PAT，便于粘到空数据的窗口启用云端。
- **空数据横幅**（`#restoreBanner`）：检测到本地空且未连云端时，顶部显示橙色提示，点它跳到数据页连云端。
- ⚠️ **已知未解问题（用户已接受）**：在用户的 iPhone 上，独立 App（主屏图标）模式底部 5 个导航被 iOS 底部 home indicator（小横条）遮挡而**不可见**，Safari 里正常。已试过 `env(safe-area-inset-bottom)` 兜底 34px，未解决。用户 2026-08-01 决定「算了，不修」。若日后重做：彻底用 `viewport-fit=cover` + 完整安全区 CSS，或直接放弃独立模式、改用 Safari 书签（那种就永远共享一份数据、不会分裂）。

---

## 8. 如何继续（新对话框开场白模板）

> 继续完善「朝夕工作台」。先读这个文件了解架构与现状：
> `C:\Users\lenovo\WorkBuddy\2026-07-31-14-53-38\fitness-coach-workbench\朝夕工作台-架构与续作指南.md`
> 以及 Obsidian 联动约定：`04-Areas · 领域/Body身体管理/健身/朝夕工作台-大脑联动约定.md`
> 本次要做的是：<你的新需求>。

新对话读完后即可直接动手，不会要求你重述架构。

---

## 9. 已知限制 & 待办（截至 v8 · 2026-08-01 收尾）

- 工作台读不到 Obsidian；「复制今日内容」与 AI机器人页均为**半自动**（需用户把汇编文本转发给大脑）。未来若要全自动，需给大脑配 Obsidian 文件读写能力（obsidian-git / MCP）。
- **护肤日志 Obsidian 未建**（类比训练日志.md）。目前护肤打卡仅进当天日记，待用户确认后由大脑实例化「护肤日志.md」。
- 周任务用「今天起 7 天相对窗口」显示，8/7 后未完成任务会从日历消失（产品审计报告 1.3 ⚠️ 待修）。
- 产品审计报告里黄/红项仍待修：历史日期添加框、菜单层级感知、进度环权重、顺延指定日期、周任务过期处理、暂停恢复提醒、放弃原因记录。
- 用户 iPhone 主屏导航遮挡（第 7 节）未解，用户已放弃。
- Gist PAT 为明文旧 token，建议用户在 GitHub 撤销重建一枚仅 gist 权限的 classic PAT。
